// src/app/page.tsx
// O middleware redireciona ANTES desta página ser renderizada.
// Este arquivo só existe para satisfazer o Next.js — nunca é exibido.
export default function RootPage() {
  return null
}
